// example of destructuring in JavaScript
const arr = [10, 20, 30];
const [x, y] = arr;
console.log(x, y)

const obj = { id: 1, value: 100 }
const { id, value } = obj;
console.log(id, value)

